<h1>Burgers!</h1>

<h2>Burgers that are not devoured!</h2>

<ul>
  <p class="burgerlist">{{#each burgers}}</p>
    {{#unless devoured}}
      {{> burgers/burger-block hungry=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block hungry=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Order Burger:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="hb">Burger:</label>
    <input type="text" id="hb" name="name">
  </div>

  <button class="orderBtn" type="submit">Place Order</button>
</form>
